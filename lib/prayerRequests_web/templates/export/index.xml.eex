<?xml version="1.0" encoding="UTF-8" ?>
<requestXML>
<%= for request <- @requests do %>
  <%= {:request, %{}, [{:name, nil, request.name}, {:place, nil, request.place}, {:sanitized, nil, request.sanitized_request}]}
  |> XmlBuilder.generate(format: :indent) %>
<% end %>
</requestXML>